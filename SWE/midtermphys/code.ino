void setup() {
    // random seed for random games or else it will 
  // Set all 7 segment output pins to OUTPUT and turn them off
  // Set all buttons as INPUT_PULLUP (button press = LOW signal) 
  // Set LED pins as outputs and ensure they start off
    
}